# next gen spec

status: archived roadmap snapshot. not the active project lane.

## current interpretation

this file captured a post-`god_run_v2` forward-looking roadmap before the
curriculum pivot and before the state-and-action-first phase-1 method was
formalized.

it is no longer the live next-step document.

the current reality is:

- curriculum chapter production is the only active lane
- paid compute is paused indefinitely
- the architecture backlog resumes only after curriculum completion
- the restart point is `biology_phase1` -> latent-world deliberation ->
  model-side neural-model evaluation

## use instead

for current project state and backlog framing, read:

1. `neuroloc/wiki/PROJECT_PLAN.md`
2. `neuroloc/spec/blueprint.md`
3. `neuroloc/HANDOFF.md`
4. `pdf_curriculum/index/curriculum_status.md`

## what remains useful here

the enduring value of the older roadmap is conceptual only:

- compression remains a central design pressure
- iterative internal computation may matter later
- multimodality should integrate into one substrate rather than bolt on

but the old file centered immediate run execution, token prediction, and
pre-pivot architectural sequencing. that framing is now historical.
